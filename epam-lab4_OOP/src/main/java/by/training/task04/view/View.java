package by.training.task04.view;

public interface View {
    void showMenu(MessageManager messageManager);

    void showAmountOfRegions(MessageManager messageManager);

    void showArea(MessageManager messageManager);

    void showRegions(MessageManager messageManager);

    void incorrectInput(MessageManager messageManager);

    //void menuFunctional(Country country);
}
